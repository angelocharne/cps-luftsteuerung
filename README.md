# cps-luftsteuerung

1. ssh hostname@ip-adress
2. sudo apt update && sudo apt upgrade
3. sudo apt install git -y
4. git clone https://github.com/angelocharne/cps-luftsteuerung.git
5. cd cps-luftsteuerung/
6. sudo docker compose up -d